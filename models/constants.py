STATE_DRAFT = 'draft'
STATE_BORROWED = 'borrowed'
STATE_RETURNED = 'returned'

BORROW_STATE_SELECTION = [
    (STATE_DRAFT, "Draft"),
    (STATE_BORROWED, "Borrowed"),
    (STATE_RETURNED, "Returned"),
]
